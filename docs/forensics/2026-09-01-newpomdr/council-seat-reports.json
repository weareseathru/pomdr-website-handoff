[
 {
  "verdict": "sound-with-amendments",
  "diagnosis_assessment": "The partial-merge diagnosis is airtight at the file layer, and I can prove it without server access. The decisive probe is HTTP Last-Modified on two static files in the SAME theme directory: style.css is Last-Modified Wed, 15 Apr 2026 (6693B, differs from repo's 17205B) while assets/css/pomdr.css is Last-Modified Tue, 25 Aug 2026 and byte-identical (cmp) to the repo. Static CSS is served raw by the webserver, so the only alternative explanation that survives HTTP-only evidence for functions.php (stale PHP opcache executing old code over new files) is falsified: no cache layer can serve April content for a static file that a full import would have replaced. The CDN-stale-cache alternative is also dead because responses mix new sidecar markup with the old enqueue set in one document, and /adopt/ dies mid-render with a live PHP fatal (I reproduced the mechanism in the repo: page-adopt.php line 237 calls pom_render_dog_card() unguarded, while page-videos.php guards with function_exists and correctly returns 200 with an empty state, exactly matching the observed 500/200 split). If any residual doubt remains, the one falsifying probe for the coworker is Appearance > Theme File Editor > divi-child > functions.php: if it does not require inc/enqueue.php, the merge is proven from inside wp-admin in ten seconds. One refinement: the failure is not only \"old files kept\", the imported wp-content also carries locally-generated Divi CSS (et-cache) with unrewritten protocol-relative //newpomdr-local.local font URLs, so the deploy pipeline has TWO independent defects, a file merge and an incomplete URL rewrite, and fixing the theme alone does not fix the second.",
  "remedy_ranking": "Ranking: B first, then C as mandatory follow-through, A only as fallback. Amendment to the dossier: A is NOT \"one-button known-good\". The same .wpress carries the et-cache CSS with //newpomdr-local.local font URLs (ai1wm missed the protocol-relative variant), so A reproduces the icon-font breakage, requires the paid Unlimited extension, re-uploads 700MB, and wipes a DB that is already correct. A is a fallback, not a peer of B.\n\nREMEDY B, exact steps for a non-developer with wp-admin access only (no CLI, no FTP):\n1. Andrew builds the zip (the inner folder must be named divi-child to match the installed slug): cd /Users/kglocker/Documents/Antigravity/pomdr-website-handoff && rm -rf /tmp/divi-child /tmp/divi-child.zip && rsync -a --exclude .git divi-child-integration/ /tmp/divi-child/ && (cd /tmp && zip -rq divi-child.zip divi-child). Send divi-child.zip to the coworker.\n2. Coworker: wp-admin > Appearance > Themes > Add New Theme > Upload Theme > Choose File (divi-child.zip) > Install Now. WordPress detects the theme already exists and shows a comparison screen; click the button \"Replace active with uploaded\". This is the WP 5.5+ atomic delete-and-replace, safer for a non-developer than deleting the live folder in a file manager (the dossier's raw-delete step B should be downgraded to the fallback if this button does not appear).\n3. Settings > Permalinks > Save Changes (change nothing, just Save) to flush rewrite rules for the pets and videos CPTs.\n4. Clear Divi generated CSS: Divi > Theme Options, find the Static CSS / file generation control (Builder > Advanced on classic panels, Performance on newer) and click Clear. If the button cannot be found, host file manager: delete the wp-content/et-cache folder (it regenerates on next load).\n5. Purge Cloudflare: the site is behind Cloudflare (cf-nel report-to headers on every response, a fact the dossier misses). Cloudflare dashboard > Caching > Configuration > Purge Everything. Then hard-refresh in the browser. Skipping this risks stakeholders seeing the broken version after a correct fix.\n6. Sanity: home shows dog cards, /adopt/ and /events/ load, no raw script text at the top of any page.\n\nREMEDY C, immediately after B, not optional: view source on the home page and one pet page and search for \"newpomdr-local\". If regenerating et-cache did not remove it, Andrew runs a DB search-replace (Better Search Replace plugin, dry-run first, or the local WP-CLI-via-socket recipe) for //newpomdr-local.local to //new.pomdr.org plus http and https scheme variants, then repeat steps 4 and 5. Note the leaked @font-face rules are INLINED in the HTML response, so this breaks icon glyphs for every visitor on first paint; it is not a lazy-loaded edge case.\n\nREMEDY A fallback: full ai1wm re-import only if B leaves mixed behavior. Watch the import run to its explicit completion screen (the observed merge is consistent with a file phase that never completed), and C is still required afterward.\n\nHardening (dossier question 3), BEFORE the redeploy because each is minutes of work and each was implicated today: function_exists guards in page-adopt.php and page-events.php (page-videos.php already proves the pattern; a hard 500 on the adopt grid is the worst possible stakeholder-facing failure for a rescue); POMDR_BUILD constant plus wp_head HTML comment; child theme Version bump 1.0.2 to 2.0.0 (also cache-busts style.css?ver= for anyone who viewed the broken bed through Cloudflare); ship the zip in the handoff folder with the README-DEPLOY replace-active instructions. AFTER redeploy but BEFORE any stakeholder demo: decide the TB header/footer record fate (see additions). After launch: the full 40-URL deploy-verify crawler.",
  "corrections": [
   "Claim 1 'DATABASE IMPORTED PERFECTLY' is overstated. The migrated site still serves //newpomdr-local.local font URLs inside Divi-generated CSS that is inlined into every HTML response. The DB rows may be consistent, but the imported data layer is not clean, and this wording would mislead a stakeholder summary into 'only files were wrong'.",
   "Claim 4 locates the icon-font leak in 'Divi cached/dynamic CSS' as if it were a separate fetched file. Verified reality: the @font-face rules for ETmodules and FontAwesome with protocol-relative local URLs are inlined directly in the homepage HTML. Consequence for remedy C: a DB search-replace alone may not remove them; et-cache must be cleared and regenerated (and Cloudflare purged) or the leak persists in the served HTML.",
   "Remedy A is described as 'one-button known-good'. It is not: the .wpress payload itself contains the local-URL et-cache CSS (ai1wm's rewrite missed protocol-relative URLs, per the dossier's own claim), so a perfect re-import still ships broken icon fonts, and it additionally depends on the paid Unlimited extension and a 700MB upload while destroying an already-correct DB.",
   "Claim 4's 'ALL dynamic islands empty (shortcodes unregistered)' lumps /videos/ in with the 500s as one failure class. Verified: page-videos.php line 14 guards with function_exists('pomdr_get_videos') and returns 200 with a designed empty state, while page-adopt.php line 237 calls pom_render_dog_card() unguarded and fatals. This distinction matters because it is live proof that hardening item D (guards) converts a hard 500 into graceful degradation.",
   "The dossier omits that new.pomdr.org is behind Cloudflare (cf-nel headers on every response). Every remedy and every verification step is unreliable without an explicit Cloudflare purge, and the version-bump cache-bust matters for the same reason."
  ],
  "additions": [
   "SENIOR-CRITICAL, missed by the technical framing: the broken bed serves Divi's zoom-lock viewport meta (verified on target: maximum-scale=1.0, user-scalable=0; local serves width=device-width, initial-scale=1). For an audience that skews older, disabled pinch-zoom is the single worst live regression on the bed today. Post-fix verification MUST include viewing source for the viewport meta AND a real pinch-zoom on a physical phone.",
   "The garbage band's content is not random: it is the LGL adoption-form prefill script (reads ?petname= and injects it as field_21 into the lglforms.com iframe), entity-mangled inside the TB header Code module (div.et_pb_code_0_tb_header). Two consequences: (a) wherever the TB header renders, that prefill logic does not execute, so the adoption-questionnaire prefill journey must be tested end-to-end post-fix (?petname= reaching the iframe field); (b) when deciding the TB record's fate, the prefill duty must provably live in the new layer first.",
   "Good news the dossier does not surface for stakeholder comms: the donation flow was never down. /donation/ returns 200 and renders the LGL iframe (iframe id=donation-iframe, verified) even in the broken state, and /donate/ is 200. Ugly, but functional. Say so explicitly; it defuses the scariest stakeholder question.",
   "pomdr-chrome.css lines 9-10 hide the TB header/footer with display:none !important, which also removes the garbage band and the duplicate h1s ('Adopt or Donate', 'Main Office', both verified live on /pets/3319/) from the accessibility tree once the CSS loads. So post-fix the band is invisible to screen readers too, but it remains one CSS failure from public display; fix or empty the TB Code module record before any public launch, not just before the demo.",
   "MINIMUM POST-FIX VISUAL VERIFICATION PROGRAM (dossier question 5). Pages, 12 in priority order: home, /adopt/, one dog detail page reached from the home grid, /donate/, /donation/ (iframe), /events/, /foster-needs/, /helping-paw/, /volunteer/, /surrender/, /adopted/, plus one native-gated D5 page that has NO sidecar template (e.g. the nested about/culture page) to prove the native gate routes to page-native.php instead of the TB-styled fallback. Breakpoints, 4: 390x844 phone, 768x1024 iPad portrait (heavy senior usage), 1280 to 1440 desktop, and desktop at 200 percent browser zoom with zero horizontal scroll (WCAG 1.4.10 reflow proxy). Per-page pass criteria: exactly one h1; new header/footer, not TB chrome; no raw 'u003cscript' text; dog cards populated with photo plus status ribbon where expected; icon glyphs render (no empty tofu boxes); body text at or above 16px; visible focus indicator when tabbing. Interactions, 6: mobile nav open/close by touch and by keyboard; skip link; tab INTO the /donation/ LGL iframe and reach a field; ?petname= prefill on the adoption questionnaire; pinch-zoom on a real phone; prefers-reduced-motion honored on the GSAP/scroll effects. Automated gates: axe-core via Chrome DevTools MCP on home, /adopt/, /donation/, and the dog detail page with zero violations vs the local baseline; curl the 40-URL crawl list for 200s; grep each page source for 'u003cscript' and 'newpomdr-local' (both must be 0) and confirm the stylesheet handle list matches local's 12; check cf-cache-status headers to confirm you are not verifying Cloudflare's stale copy.",
   "STAKEHOLDER FRAMING (calm, non-technical, accurate, no em dashes): 'All content, dog records, photos, and the donation form transferred correctly, and donations were never interrupted. During the copy, the new design's files arrived but several older files with the same names were kept instead of replaced, so the test site ran a mix of two versions. That mix is why pages looked unstyled, two pages showed errors, and the dog listings appeared empty. Nothing was lost. The complete, correct version runs on our build machine, and the fix is a one-step theme replacement that we have rehearsed, followed by a page-by-page visual check before anyone is invited to look again.' Do not demo until the verification program above passes; a stakeholder who has seen the broken bed should be given a hard-refresh instruction or a fresh browser profile because Cloudflare and browser caches can replay the broken version.",
   "Add to the deploy-verify script: assert the rendered viewport meta equals 'width=device-width, initial-scale=1' (zoom lock regression detector), assert 0 occurrences of 'u003cscript' and 'newpomdr-local' in served HTML, and assert h1 count == 1 per page. These three are cheap greps that would each have caught today's most user-facing failures."
  ],
  "verified_claims": [
   "500s: curl -o /dev/null -w '%{http_code}' on https://new.pomdr.org/adopt/ and /events/ both return 500; /donate/, /donation/, /videos/, /adopted/, /courtesy-listings/, /about/, /volunteer/, /helping-paw/, /surrender/ all 200; /foster/ 301 to /foster-needs/.",
   "Old functions.php behavior on target: curl of https://new.pomdr.org/ shows exactly one child-theme stylesheet enqueued (divi-child/style.css?ver=1.0.2); curl of http://newpomdr-local.local/ shows 12 design stylesheets including tokens.css, pomdr.css, pomdr-chrome.css, a11y.css, plus the Google Fonts link.",
   "Partial merge on disk: curl -sI https://new.pomdr.org/wp-content/themes/divi-child/style.css gives Last-Modified Wed, 15 Apr 2026 (raw size 6693B vs repo 17205B at /Users/kglocker/Documents/Antigravity/pomdr-website-handoff/divi-child-integration/style.css); curl of .../assets/css/pomdr.css gives Last-Modified Tue, 25 Aug 2026, 57196B, and cmp confirms it is byte-identical to the repo copy.",
   "Garbage band: literal 'u003cscriptu003en...' text present inside <div class='et_pb_code_0_tb_header ... et_pb_code'> in the target home HTML; the same string appears once in the local home HTML but local also enqueues pomdr-chrome.css, whose lines 9-10 hide .et-l--header and .et-l--footer with display:none !important. The mangled content is the lglforms.com petname prefill script (field_21, adoption-iframe).",
   "Icon-font leak: grep of the target homepage HTML shows inline @font-face rules with src url(//newpomdr-local.local/wp-content/themes/Divi/core/admin/fonts/...) for ETmodules and FontAwesome (fa-regular-400, fa-solid-900, fa-brands-400).",
   "Zoom lock: target home serves <meta name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0'>; local serves width=device-width, initial-scale=1.",
   "Dynamic islands: 0 occurrences of 'dog-card' in target home HTML vs 12 in local home HTML; https://new.pomdr.org/wp-json/wp/v2/pets responds (pets CPT alive), and /pets/3319/ renders 200 with duplicate h1s ('Adopt or Donate', 'Main Office') from the TB footer.",
   "500 mechanism in repo: page-adopt.php line 237 calls pom_render_dog_card() with no function_exists guard; page-videos.php line 14 guards pomdr_get_videos() with function_exists, matching the observed 500 vs 200 split; page-donation.php builds the LGL iframe inline, and the served /donation/ page contains iframe id='donation-iframe'.",
   "Cloudflare fronting: report-to cf-nel headers present on new.pomdr.org responses (seen on the direct functions.php probe)."
  ]
 },
 {
  "verdict": "sound-with-amendments",
  "diagnosis_assessment": "The partial-merge diagnosis is essentially airtight for the executing code: the child theme on new.pomdr.org runs the April 15 2026 functions.php and style.css while new redesign files sit on disk unloaded. I re-proved it three independent ways: (1) target style.css is 6693 bytes with Last-Modified Wed 15 Apr 2026 while the repo file is 17205 bytes; (2) new files ARE on disk (curl 200 for inc/native-gate.php, assets/css/pomdr.css, assets/css/tokens.css, assets/js/a11y.js) yet zero pomdr-* handles appear in any page HTML, proving the loader (functions.php requires at lines 2277-2281) is the old file; (3) the fatals land exactly where the old file lacks redesign-era functions: /adopt/ output stops at the literal string '<div class=\"dogs-grid\" id=\"dogs-grid\">' immediately before page-adopt.php line 237 (undefined pom_render_dog_card, added post-April in commit db0a534), and /events/ dies right after the Theme Builder header, before any template markup, at page-events.php line 16 (undefined pomdr_collect_events, added in commit 59fa698). The only alternative that survives is stale OPcache (validate_timestamps=0) serving old functions.php over a NEW on-disk file; but style.css is served statically and is provably old on disk, so opcache alone cannot explain the evidence, it would need to co-occur with a partial file copy anyway. The one falsifying probe: have the coworker open the hosting file manager and read the modified date and size of wp-content/themes/divi-child/functions.php. An April date confirms the merge; an August date means opcache, and the fix becomes bounce PHP-FPM / purge opcache instead of re-upload. Remedy B below covers both branches, so the distinction does not change the plan. The dossier's deploy-method inference is appropriately hedged; note it is equally consistent with the coworker never running ai1wm at all (manual DB import plus FTP/file-manager merge), which the 512MB free-import limit makes plausible.",
  "remedy_ranking": "Rank: B first, then C conditionally, A only as fallback. D-minimal items ship inside the B package.\n\nB (surgical theme replacement, no CLI needed). Steps for the coworker, prepared by Andrew:\n1. Andrew packages the zip with the ROOT FOLDER RENAMED TO divi-child (critical trap, see additions): rsync -a --exclude .git divi-child-integration/ /tmp/divi-child/ then (cd /tmp && zip -r divi-child.zip divi-child). Before zipping, apply the pre-deploy hardening set (style.css Version 2.0.0, POMDR_BUILD fingerprint, the 3 function_exists guards).\n2. Coworker: wp-admin > Appearance > Themes > Add New Theme > Upload Theme > choose divi-child.zip > Install Now. WordPress detects the theme is already installed and shows a comparison screen (it will read 'You are uploading version 2.0.0, current is 1.0.2' after the bump); click 'Replace active with uploaded'. This is the WP 5.5+ true-replace flow, it deletes the old folder.\n3. If that screen errors ('Destination folder already exists' with no replace button, some hosts), fallback: hosting file manager, delete the entire wp-content/themes/divi-child folder, then repeat step 2 as a fresh install and Activate 'Divi Child'.\n4. Settings > Permalinks > Save Changes (registers pets/events/videos rewrites from inc/post-types.php and inc/videos.php, which had never loaded).\n5. Purge Divi's CSS cache: hosting file manager, delete the CONTENTS of wp-content/et-cache (Divi rebuilds it), or Divi > Theme Options > Performance > clear cache.\n6. Purge host/CDN cache (Cloudflare is in front, cf headers observed) and PHP OPcache if the panel exposes it (toggling the PHP version bounces FPM on most hosts).\n7. Andrew runs the external verify crawl from his machine (no server access needed): every URL 200, POMDR build comment present, pomdr-tokens link present, no literal '[' shortcode text, dog cards > 0 on home/adopt/foster-needs/adopted, no 'newpomdr-local.local' anywhere.\n\nC (data hygiene, run only if step 7 still finds local-host strings): the icon-font leak is in Divi's INLINE dynamic CSS printed into the HTML, not in the linked et-cache files (I grepped all three et-cache files for page 54, clean), so purging et-cache in step 5 may regenerate it correctly. If //newpomdr-local.local persists, install the free Better Search Replace plugin on the test bed, dry-run then run: search '//newpomdr-local.local' replace '//new.pomdr.org' across all tables (serialization-safe; this also catches http:// and https:// variants because they contain the protocol-relative substring). Then repeat steps 5-7.\n\nA (full ai1wm re-import) is the fallback, not the lead: it requires the Unlimited extension for the ~700MB payload (likely the exact wall that caused the improvised merge), it wipes a DB that is already verified correct, and it re-imports the same local-host font URLs so C is needed afterward anyway. Use it only if B fails twice.\n\nNote for the verify pass: after B, the restored inc/native-gate.php routes the 29 _pomdr_native pages (including /events/ and home, cutover records dated 2026-08-25 before the DB export) to page-native.php. Expect native rendering there, not the sidecar; page-events.php becomes dead code on that route.",
  "corrections": [
   "Claim 4 'ALL dynamic islands EMPTY ... shortcodes unregistered' is wrong for the home page: [pet_home] IS registered in the old functions.php and renders a POPULATED grid in the old markup (custom-acf-posts-grid-4 / custom-post-item, real dogs, integer URLs like /pets/2865/). Only redesign-era shortcodes are missing; /foster-needs/ proves that by printing the literal text [foster_needed_dogs]. 'dog-card count = 0' is true only for the new .dog-card class and is misleading as evidence of empty islands.",
   "The icon-font leak is NOT in the linked et-cache/dynamic CSS files: I fetched all three et-cache stylesheets referenced by the home page and none contain newpomdr-local.local. The @font-face block with //newpomdr-local.local URLs is INLINE in the page HTML (Divi's cached inline dynamic CSS, also present on local with the correct host). Consequence: remedy C must purge et-cache AND, if strings persist, search-replace the DB; replacing theme files fixes nothing here.",
   "Claim 4's 'where none exists, page.php + TB template serves the D5 content' applies to zero of the 29 cutover pages: every cutover slug has a sidecar mask (home has front-page.php). The branch only applies to non-cutover D5 records such as staging pages. Minor, but the 29-native check in the verify design should assert native markers, not sidecar markers, after the fix.",
   "The 'page enqueues ONLY divi-child/style.css?ver=1.0.2' wording invites reading ?ver as proof of the old file. It is not: BOTH the old (6693B) and new (17205B) style.css declare Version 1.0.2, so ver=1.0.2 discriminates nothing (the dossier's content-diff is the valid proof). This is also why the 2.0.0 bump is load-bearing, not cosmetic.",
   "Dossier claim 5 leans on ai1wm having run at all; the evidence is equally consistent with no ai1wm import ever succeeding (free-tier 512MB block on a 700MB payload) and a manual DB import plus FTP/file-manager merge. Harmless to the remedies, but do not assert the tool to the coworker."
  ],
  "additions": [
   "ZIP ROOT FOLDER TRAP (dossier misses this, it can silently reproduce the failure): the repo folder is divi-child-integration but the installed theme slug is divi-child. A zip built straight from the repo folder installs as a SECOND theme alongside the broken one, which stays active, and the coworker reports 'I uploaded it' while nothing changes. The packaging step must rename the zip root to divi-child.",
   "Single point of silent failure in inc/enqueue.php line 34: the entire design chain (pomdr-tokens -> pomdr-design -> pomdr-shared -> boosts -> chrome) depends on the 'divi-style' handle registered by dt_enqueue_styles (functions.php lines 139-154). If Divi 5 ever renames or drops that handle, WordPress silently drops every stylesheet in the chain, reproducing exactly this 'renders but unstyled' symptom with zero errors. Add a 3-line guard: if ! wp_style_is('divi-style','registered') then wp_register_style it against the parent style.css.",
   "Guard scope is 2 files, not 40: page-videos.php line 14 and single-pets.php (inline definitions plus function_exists at lines 189, 198, 284) already model the correct pattern. Only page-adopt.php (pom_render_dog_card at lines 237 and 277) and page-events.php (pomdr_collect_events line 16, pomdr_render_events lines 33 and 41) fatal. Blanket-guarding all sidecars is unnecessary churn; guard those five call sites with a visible 'this page is being updated' fallback so a partial deploy degrades in front of stakeholders instead of 500ing, and rely on the fingerprint plus crawl for detection so the soft-fail never masks a bad deploy.",
   "mu-plugin question (my seat's call): moving critical registrations to an mu-plugin WOULD have survived this exact failure (mu-plugins are auto-included by core, they have no stale loader file), but it breaks the one-artifact 'upload theme zip' story this team's non-developer deploy depends on (Appearance > Themes upload cannot install mu-plugins, so you create a second artifact that can itself go stale). Verdict: keep registrations in the theme; the replace-not-merge flow plus fingerprint plus crawl closes the hole. At most, a 20-line mu-plugin sentinel that admin-notices when POMDR_BUILD is undefined is defensible; wholesale relocation is over-engineering.",
   "Over-engineering flags in D: (a) the server-side eval checks ('inc/ files loaded, shortcodes registered, gate-armed count = 29, enqueue handles present') are unusable by a coworker without CLI and redundant with an external crawl that asserts rendered outcomes; keep them dev-only or drop them. (b) The admin-footer stamp is redundant once style.css says 2.0.0 (visible on the Themes screen) and wp_head carries the build comment. (c) Minimal permanent set: replace-not-merge zip flow with correct root folder, POMDR_BUILD constant + wp_head comment that also prints filemtime of assets/css/pomdr.css (absence of the comment = stale functions.php, mismatched mtime = stale assets), Version 2.0.0 bump, the 5 call-site guards, README-DEPLOY 'delete the theme folder first, never merge', and the owner-run 40-URL crawl. Everything else is optional.",
   "OPcache and CDN purge are missing from remedy B as written: a file replace can keep serving old PHP from opcache, and Cloudflare (cf response headers confirmed on target) can keep serving old CSS, especially since both style.css versions are 1.0.2 and filemtime-based ?ver strings survive if the unzip preserves mtimes. Make purge an explicit numbered step.",
   "Timestamps corroborate that the target DB already carries the 29 native cutovers (scripts/native/backups/cutover-*.json all 2026-08-25 18:14-18:15Z, before the exported page-54 modified time), so no DB action is needed for the gate to work after B; and the verify crawl should expect /events/ and home to render via page-native.php, not their sidecars."
  ],
  "verified_claims": [
   "/adopt/ and /events/ hard 500, /videos/ and / 200: curl -s -o /dev/null -w '%{http_code} %{size_download}' https://new.pomdr.org/{adopt,events,videos,}/ (500/135524B, 500/126991B, 200, 200); both 500 bodies end with the WordPress critical-error die page appended after partial HTML.",
   "Exact fatal locations: python extraction of the /adopt/ body shows output stops at '<div class=\"dogs-grid\" id=\"dogs-grid\">', the line immediately before page-adopt.php:237 'echo pom_render_dog_card(...)'; /events/ body ends right after the TB header with no 'page-headline', matching pomdr_collect_events() at page-events.php:16; both functions exist only in the new functions.php (lines 687 and 880; git log -S shows they were added in commits db0a534 and 59fa698, after the April production file).",
   "Old style.css on target disk: curl -sI https://new.pomdr.org/wp-content/themes/divi-child/style.css returns Last-Modified Wed, 15 Apr 2026 21:14:06 GMT and 6693 bytes, versus the repo file at 17205 bytes; both declare Version 1.0.2 (head -20 of repo style.css), so ?ver cannot discriminate old from new.",
   "New files present on target but unloaded: curl status 200 for wp-content/themes/divi-child/inc/native-gate.php, assets/css/pomdr.css, assets/css/tokens.css, assets/js/a11y.js; meanwhile the full home HTML contains only themes/divi-child/style.css?ver=1.0.2 from the child theme, zero pomdr-* handles, zero design JS, and Divi's zoom-locked viewport meta (maximum-scale=1.0, user-scalable=0), proving the old functions.php executes; local home for comparison enqueues the full 18-file design chain (curl http://newpomdr-local.local/).",
   "Home dogs island NOT empty (dossier correction): extracted the region between 'Find your new' and 'video-section' in the target home HTML; it renders custom-acf-posts-grid-4 with real dogs (Patches, Chocolate Chunk, Suki) linking to integer URLs like /pets/2865/, i.e. the OLD [pet_home] implementation, not an empty island; /foster-needs/ conversely renders the literal text [foster_needed_dogs] (curl + grep), and /pets/2865/ returns 200 because single-pets.php defines its helpers inline and guards the rest.",
   "Icon-font leak location: grep of saved target home and adopt HTML finds inline @font-face URLs //newpomdr-local.local/wp-content/themes/Divi/core/admin/fonts/fontawesome/fa-brands-400.* ; the same block appears in LOCAL home HTML (correct host there); the three linked et-cache CSS files for page 54 contain NO newpomdr-local references (curl + grep of each), so the leak lives in Divi's inline/DB-cached dynamic CSS.",
   "u003cscript TB header garbage: present exactly once in target home HTML and once in local home HTML (grep -c), confirming the latent-on-local, exposed-on-target claim.",
   "29 native cutovers, all masked by sidecars: ls scripts/native/backups/cutover-*.json yields 29 unique slugs (including events and home); cross-checked every slug against the theme's page-{slug}.php files (home against front-page.php), all have sidecar masks; requires at functions.php:2277-2281 and the divi-style dependency at inc/enqueue.php:34 read directly from the repo."
  ]
 },
 {
  "verdict": "sound-with-amendments",
  "diagnosis_assessment": "The partial-merge diagnosis is correct at the on-disk level and I upgraded it from inference to proof, but the dossier's mechanism inference picks the wrong lead candidate. Proven state: cache-busted origin fetches (cf-cache-status: MISS, so Cloudflare is ruled out) show old style.css (6693B, Last-Modified Apr 15 2026 21:14:06 GMT) coexisting with byte-identical-to-repo new files (pomdr.css 57196B) in the same divi-child directory. Stale opcache is falsified because style.css is a static file served straight from disk, no PHP involved, and it is old at origin. Partial extraction that died mid-run is falsified because ai1wm imports the database AFTER the file phase, and the DB is complete and URL-rewritten. The decisive new evidence: I parsed the actual .wpress the coworker used (found at /Users/kglocker/Downloads/newpomdr-local-local-20260825-121945-wyjhpxo0bglf.wpress, 911MB). It CONTAINS the correct new theme files (themes/divi-child/style.css 17205B, functions.php 82776B, all inc/*.php), every entry stamped mtime 2026-08-25 18:15:50 GMT. The target serves its new files with Last-Modified 25 Aug 2026 18:15:50 GMT, matching the archive to the second. ai1wm's extractor restores archived mtimes; FTP or file-manager uploads would not reproduce that stamp. Conclusion: the ai1wm import RAN on the target (so the coworker had Unlimited or equivalent, a 911MB file cannot enter the free importer), the extractor successfully created files at new paths but failed to overwrite every pre-existing file, and the DB phase then completed normally. The leading mechanism is per-file overwrite failure on foreign-owned pre-existing files: PHP could create files in divi-child/ (proven, the new files exist) but could not fopen('wb') existing files owned by another uid, and the extractor continued past the failures. The one probe that would confirm or falsify the permission mechanism: in the host file manager, compare owner/group/permissions of divi-child/style.css (pre-existing) against divi-child/assets/css/pomdr.css (created by import); different owners confirms it. Also check ai1wm's import error log under wp-content/ai1wm-backups/. This mechanism matters because it inverts the remedy ranking: a re-import (remedy A) would hit the same unwritable files and reproduce the failure identically, while delete-then-install (remedy B) works because unlinking a file needs only write permission on the DIRECTORY, which the PHP user demonstrably has.",
  "remedy_ranking": "RANKING: B first (surgical theme replace), then C (cache regeneration, then targeted search-replace only if needed), A only as a last-resort fallback and only after the permission question is resolved, because under the proven mechanism A likely reproduces the exact failure. The DB is verified correct, so wiping it again (A) risks much to fix nothing.\n\nANDREW'S PREP BEFORE HANDING OFF (30 minutes, do before the coworker touches anything): (1) Bump divi-child-integration/style.css Version 1.0.2 to 2.0.0. This is not cosmetic: both old and new style.css currently claim 1.0.2, the site is behind Cloudflare with max-age 14400 on CSS, and an unchanged ?ver=1.0.2 URL will keep serving the OLD stylesheet from the edge for up to 4 hours after the origin is fixed. (2) Add a POMDR_BUILD constant plus a wp_head HTML comment so staleness is visible in view-source. (3) Add function_exists guards around pom_render_dog_card in page-adopt.php and around pomdr_collect_events / pomdr_render_events in page-events.php so a future partial deploy degrades instead of 500ing. (4) Build the zip: the top-level folder INSIDE the zip must be named exactly divi-child, not divi-child-integration. If the folder name differs, WordPress installs a second theme alongside and the broken divi-child stays active. Verify the zip has no symlinks and contains functions.php at 82776B-plus-your-edits and style.css showing 2.0.0.\n\nCOWORKER PROCEDURE, REMEDY B (wp-admin only, no CLI, about 15 minutes):\n1. Log in to new.pomdr.org wp-admin. Go to Appearance, then Themes, then Add New Theme, then Upload Theme.\n2. Choose the divi-child.zip Andrew sent. Click Install Now.\n3. WordPress will say the theme is already installed and show a comparison (current version 1.0.2, uploaded 2.0.0). Click the button labeled \"Replace active with uploaded\". This deletes the old theme folder entirely and installs the fresh copy, which is exactly what we need (never merge).\n4. If you instead see an error like \"Could not remove the old theme\", STOP. Do not retry. Tell Andrew: it means the old files are owned by a different server user and the hosting company must fix file ownership on wp-content/themes/divi-child. This same ownership problem is what broke the original import.\n5. Go to Settings, then Permalinks, change nothing, click Save Changes. (Required: the new theme registers the videos post type and its URLs need this flush.)\n6. Go to Divi, then Theme Options, find the Builder or Performance tab, and click Clear next to Static CSS File Generation, then Save Changes. (This regenerates Divi's cached CSS, which currently contains addresses pointing at Andrew's computer.)\n7. In the hosting control panel, purge any server cache; if there is a \"Restart PHP\" or \"Flush opcache\" button, click it. If you have Cloudflare access for new.pomdr.org: Caching, then Configuration, then Purge Everything. If not, tell Andrew so he can.\n8. Verify, in this order: Appearance > Themes shows Divi Child version 2.0.0. The home page shows the styled design with six dog cards and ONE header and ONE footer. /adopt/ loads with no \"critical error\" message and the filters work. /events/ loads. /videos/ shows the video grid. Then run the 10-minute list in docs/DEPLOY-README-FOR-COWORKER.md. Hard-refresh each page (Cmd+Shift+R) so your browser is not showing old files.\n\nREMEDY C, AFTER B (Andrew, or coworker with guidance): first re-check three pages' HTML for the string newpomdr-local.local after step 6's cache clear, since regeneration from the now-correct site URL will usually purge the leaked font URLs on its own. If any remain, install the Better Search Replace plugin, dry-run replacing newpomdr-local.local with new.pomdr.org across all tables (the bare hostname catches the protocol-relative //newpomdr-local.local form that ai1wm's rewriter missed, and the plugin is serialized-data safe), review the dry-run counts, run it live, then delete the plugin. This replaces the dossier's wp search-replace step, which the coworker cannot run without CLI access.\n\nREMEDY A, FALLBACK ONLY: full re-import of the same .wpress with the Unlimited extension. I verified the archive itself is good (it contains all the new theme files), so A is safe content-wise, but do it only after host support confirms or fixes file ownership under wp-content, otherwise the extractor will skip the same unwritable files again and you will rebuild the identical broken state. Also note A does not delete target files that are absent from the archive; it overwrites matching paths and imports the DB.\n\nHARDENING TIMING (dossier question 3): BEFORE redeploy: version bump, build constant plus wp_head comment, function_exists guards, correctly named standalone zip, README-DEPLOY edits (delete-or-replace never merge, plus the \"Could not remove the old theme\" escalation path, plus Cloudflare purge step). AFTER the test bed is green: the 40-URL deploy-verify script, admin-footer stamp, and the TB header/footer record decision (the escaped-script Code module stays latent once pomdr-chrome.css is back; fix the record on your own schedule, it needs the sidecar-vs-native suppression risk analysis first).",
  "corrections": [
   "Dossier point 5's lead mechanism is wrong. It rates 'file-manager unzip / FTP merge' as most consistent, but the target's new files carry Last-Modified 2026-08-25 18:15:50 GMT, matching the .wpress entry mtimes to the exact second. Only ai1wm's extractor restores archived mtimes like that. The import ran; its extractor failed to overwrite pre-existing files (ownership/permissions is the leading cause) while creating new paths fine, then the DB phase completed. Consequence: remedy A as written ('one-button known-good') would likely reproduce the failure and is NOT known-good until host file ownership is fixed.",
   "'A full ai1wm import extracts and REPLACES all files' is overstated. ai1wm overwrites files that exist in the archive at matching paths and does NOT delete target-only files; the clean-wipe semantics apply to the database (tables replaced), not the filesystem.",
   "The icon-font leak is NOT in the linked et-cache CSS files. I grepped all three home-page et-cache files (et-divi-dynamic-tb-145-tb-307-54.css and both et-core-unified files) and they are clean. The //newpomdr-local.local @font-face URLs live in INLINE style CSS printed into the HTML from Divi's DB-cached generated CSS. Fix implication: clearing Divi's generated CSS cache (Theme Options clear + save) regenerates it from the now-correct siteurl and probably fixes it without DB surgery; delete-et-cache alone would not.",
   "Payload size: the archive is 911MB, not ~700MB (ls -lh on the actual .wpress). Does not change the free-vs-Unlimited verdict, both exceed 512MB, but the README should be corrected.",
   "The dossier's remedy B cache list omits Cloudflare entirely. new.pomdr.org is behind Cloudflare (cf-cache-status: HIT, age 506, cache-control max-age=14400 observed on style.css). Because old and new style.css both declare Version 1.0.2, the ?ver=1.0.2 URL would keep serving the stale edge copy for up to 4 hours after any fix. The version bump must happen BEFORE the redeploy zip is built, and Purge Everything should be in the runbook.",
   "Trivial: the dossier's REST timestamps (11:27:34, 11:14:58) are local time; the API returns 2026-08-25T18:27:34 and 18:14:58 GMT. Substance verified correct, target matches local to the second."
  ],
  "additions": [
   "ARCHIVE VERIFIED GOOD (new work, closes the dossier's biggest open premise): parsed the wpress block headers of /Users/kglocker/Downloads/newpomdr-local-local-20260825-121945-wyjhpxo0bglf.wpress. It contains themes/divi-child/style.css (17205B), functions.php (82776B, same byte size as repo), page-native.php, page-adopt.php, and all six inc/*.php files including post-types.php and redirects.php. No re-export is needed; the export pipeline (including the symlinked theme dir on Local) worked.",
   "Zip packaging trap for remedy B: the repo folder is divi-child-integration but the deployed slug is divi-child. The redeploy zip's internal top-level folder must be exactly divi-child or WordPress installs a parallel theme and the broken one remains active. This is the single easiest way for remedy B to silently fail.",
   "The 'Could not remove the old theme' error during the Replace flow is diagnostic gold: it confirms the foreign-ownership mechanism and the escalation is host support fixing ownership on wp-content/themes/divi-child, after which both B and A become viable.",
   "Plugins were spot-checked for the same skip-existing failure: ACF Pro readme.txt on target is Aug 20 (matches archive era, local was updated Aug 28 after the export), Redirection matches local to the second. Plugin files appear to have been written by the import, so the failure looks scoped to the pre-existing divi-child files, but verify ACF's version in wp-admin after the fix.",
   "Someone touched the target TODAY: all-in-one-wp-migration/readme.txt on new.pomdr.org has Last-Modified 2026-09-01 21:28:49 GMT, a few hours old. Coordinate with the coworker before redeploying so two people are not operating on the test bed simultaneously.",
   "ai1wm free-tier import facts as of 2026, precisely: exports are unlimited and free; the browser import path is capped at 512MB in the free plugin and further limited by the host's upload_max_filesize / post_max_size below that; the Unlimited Extension (USD 69, servmask.com) removes the cap; current-2026 wordpress.org reviews confirm restoring the plugin's own large backups is pay-walled in the free tier, so the FTP-into-ai1wm-backups Backups-page restore is not a reliable free workaround in current builds; the old downgrade-to-v6.x bypass is unsafe and should never be handed to a non-developer. The 911MB archive genuinely required Unlimited, and the fact the import demonstrably ran implies the coworker had it (or an equivalent), worth confirming with them. Sources: [ProfileTree on the 512MB limit](https://profiletree.com/wp-all-in-one-migration/), [wordpress.org plugin page and reviews](https://wordpress.org/plugins/all-in-one-wp-migration/), [Unlimited Extension overview](https://flawlessthemes.com/all-in-one-wp-migration-unlimited-extension/)",
   "Opcache nuance for the runbook: most hosts leave opcache.validate_timestamps on (default, 2s revalidation), so the theme replace takes effect immediately; if the host disables it, the new wp_head build comment makes the staleness visible in seconds and the fix is the control panel's PHP restart button. Do not let anyone chase 'mystery caching' without checking the build comment first.",
   "Post-fix leak probe to add to verification: curl the home page plus two content pages and grep for newpomdr-local; expect zero after the Divi CSS clear, and only reach for Better Search Replace if nonzero.",
   "The permalink-flush step is load-bearing, not ritual: inc/post-types.php (videos CPT) has never executed on the target, so its rewrite rules do not exist there; without Settings > Permalinks > Save after the theme replace, /videos/ singles will 404 even with everything else fixed."
  ],
  "verified_claims": [
   "Old style.css at ORIGIN, not CDN: curl -D - 'https://new.pomdr.org/wp-content/themes/divi-child/style.css?councilbust=<ts>' returned cf-cache-status: MISS, Last-Modified Wed 15 Apr 2026 21:14:06 GMT, body 6693B with 'Version: 1.0.2' header; repo file /Users/kglocker/Documents/Antigravity/pomdr-website-handoff/divi-child-integration/style.css is 17205B. Also falsifies the CDN-cache and opcache alternatives in one shot (static file, cache-busted).",
   "New files current and byte-identical: cache-busted curl of assets/css/pomdr.css (cf-cache-status: MISS, Last-Modified Tue 25 Aug 2026 18:15:50 GMT, 57196B) then diff -q against the repo copy: identical. Also probed page-native.php, inc/native-gate.php, inc/enqueue.php, inc/chrome.php, inc/videos.php, pomdr-chrome.css (23215B), native-boost.css (149471B), native-pages.js (2688B): all present on target.",
   "Old functions.php behavior on the rendered home page: curl of https://new.pomdr.org/ shows the only child-theme stylesheet is divi-child/style.css?ver=1.0.2 (plus Divi et-cache files), zero references to pomdr.css/native-boost/pomdr-chrome, zero a11y.js/pomdr-nav.js/native-pages.js, Divi viewport meta with maximum-scale=1.0 user-scalable=0 still present, dog-card count 0, one raw u003cscript occurrence, duplicate h1s including 'Adopt or Donate' from the TB footer.",
   "Hard 500s and mechanism: curl -w gave /adopt/ 500 with 135524B body and /events/ 500 with 126991B body, both ending in the WordPress critical-error document after partial page HTML. Repo cross-check: page-adopt.php calls pom_render_dog_card (defined only in new functions.php line 687), page-events.php calls pomdr_collect_events / pomdr_render_events (new functions.php lines 880/943), and new functions.php lines 2277-2281 are the require_once calls for inc/chrome.php, inc/enqueue.php, inc/videos.php, inc/post-types.php, inc/native-gate.php, none of which the old functions.php loads.",
   "DB parity: curl https://new.pomdr.org/wp-json/wp/v2/pages/54?_fields=id,modified_gmt and the same against http://newpomdr-local.local both returned modified_gmt 2026-08-25T18:27:34; target page 987 returned 2026-08-25T18:14:58. Local site also serves the new 17205B style.css.",
   "Icon-font leak relocated: grep of the three linked et-cache CSS files found NO newpomdr-local.local, but the home page HTML's inline CSS contains the full protocol-relative @font-face set (//newpomdr-local.local/wp-content/themes/Divi/core/admin/fonts/fontawesome/fa-brands-400.woff2 etc.).",
   "Archive contents (new probe the dossier lacked): python parse of the wpress block headers (255B name, 14B size, 12B mtime, 4096B path) of /Users/kglocker/Downloads/newpomdr-local-local-20260825-121945-wyjhpxo0bglf.wpress (911MB) shows themes/divi-child/style.css 17205B, functions.php 82776B, pomdr.css 57196B, all mtime 2026-08-25 18:15:50 GMT, matching the target's served Last-Modified for the new files to the second. This is the fingerprint proving ai1wm extraction wrote the target's new files.",
   "ai1wm free import limits as of 2026: WebSearch plus WebFetch of https://wordpress.org/plugins/all-in-one-wp-migration/ (free import ceiling 512MB, Unlimited removes it, Aug 2026 reviews complaining import/restore of its own backups is pay-walled) and https://profiletree.com/wp-all-in-one-migration/; confirms the README's Unlimited requirement for this archive is correct, though the stated ~700MB size is not (911MB)."
  ]
 },
 {
  "verdict": "sound-with-amendments",
  "diagnosis_assessment": "The partial-merge root cause is airtight after my independent probes. Three alternatives were considered and all are falsified by evidence I gathered myself. (1) Two-themes-installed hypothesis (new theme uploaded as a second folder, old one still active): falsified because the NEW files live inside the ACTIVE theme folder (https://new.pomdr.org/wp-content/themes/divi-child/assets/css/pomdr.css is byte-identical to the repo, and inc/enqueue.php, inc/chrome.php, inc/native-gate.php, inc/videos.php all return 200-blank ABSPATH-guard exits at that same path, while page-native.php 500s when hit directly, proving it exists there) and the sidecar templates demonstrably execute. (2) Stale-opcache hypothesis (all files new, PHP serving old bytecode): falsified because style.css is served statically, bypassing PHP, and the target serves the 6693-byte April file with Last-Modified Apr 15 2026 behind a Cloudflare max-age of 14400s, so the FILE on origin disk is old; meanwhile tokens.css and pomdr.css show Last-Modified Aug 25 18:15 GMT. Mixed mtimes inside one active theme folder is a merge, full stop. (3) Old-build-of-new-theme hypothesis: falsified because pomdr.css is byte-identical to the current repo (diff clean) while functions.php behaves exactly like the April production child theme (only dt_enqueue_styles output present). The one remaining unknown is WHICH tool produced skip-existing semantics (FTP-merge, file-manager extract choice, or aborted extraction); that is unknowable remotely and does not change the remedy. The single probe that would have falsified (or now conclusively documents) the diagnosis once anyone has file access: ls -la wp-content/themes/divi-child on the target; a mix of April and Aug 25 mtimes in one listing is the whole story on one screen. I also add one proven refinement the dossier missed: wp-content/et-cache was carried over from local in the same deploy (target serves et-cache/54/et-divi-dynamic-tb-145-tb-307-54-critical.css, 41356 bytes, Last-Modified Aug 25 18:28:06 GMT, which equals the ?ver=1787682486 stamp on every et-cache link), which is what makes the icon-font leak a FILE problem, not a DB problem.",
  "remedy_ranking": "RANKING: B first (surgical theme replace), then the cache purge (amended C), then verify; A only as fallback, and A ALSO requires the cache purge. Reasoning from this seat: the DB is proven correct, all failures trace to two old PHP files plus poisoned et-cache, B is an 11MB browser upload with no paid extension, and A would re-import the poisoned et-cache anyway (a fresh ai1wm export carries wp-content/et-cache with the local hostname baked in unless someone finds the exclude-cache advanced option).\n\nSTEP-BY-STEP FOR THE COWORKER (browser only, no CLI):\n\nStep 0 (Andrew does this, before handing over): build the zip with the CORRECT FOLDER NAME. The active theme folder on the target is \"divi-child\" but the repo folder is \"divi-child-integration\". If the zip's top-level folder is not exactly \"divi-child\", WordPress installs a SECOND theme and the replace flow never triggers; the site keeps rendering the old theme and the fix silently does nothing. Also bump style.css Version to 2.0.0 first (see corrections; both old and new currently say 1.0.2, which is why this failure was invisible for a week and why browser/Cloudflare caches would keep serving the old style.css even after a correct replace). Commands for Andrew: cp -r divi-child-integration /tmp/divi-child && cd /tmp && zip -r divi-child.zip divi-child -x '*.git*' '*node_modules*'.\n\nStep 1: wp-admin > Appearance > Themes > Add New Theme > Upload Theme > Choose File (divi-child.zip) > Install Now.\n\nStep 2: WordPress will say \"This theme is already installed\" and show a current-vs-uploaded comparison table (with the version bump it reads 1.0.2 vs 2.0.0, so the coworker can SEE they have the right zip). Click the button \"Replace active with uploaded\". Do NOT use any FTP or file-manager merge, ever; if this flow is unavailable, delete the divi-child folder entirely first, then upload fresh. Never merge.\n\nStep 3 (mandatory, this is what actually fixes the broken icons; the theme replace alone does NOT touch it): purge Divi's generated CSS. wp-admin > Divi > Theme Options > Builder tab > Advanced > \"Static CSS File Generation\" > click \"Clear\" then Save Changes. If that button cannot be found in the Divi 5 options UI, the equivalent is deleting the folder wp-content/et-cache via the host's file manager (completely safe, Divi regenerates it on the next page view, and the regenerated files will carry new.pomdr.org instead of newpomdr-local.local).\n\nStep 4: wp-admin > Settings > Permalinks > click Save Changes (no edits needed). This flushes rewrite rules so the pets and videos post types and native routes resolve.\n\nStep 5: purge Cloudflare if they have dashboard access (Caching > Purge Everything). The target fronts through Cloudflare with max-age 14400 on theme CSS; skipping this can make verification lie for up to 4 hours.\n\nStep 6, verification a non-developer can do: open https://new.pomdr.org/ and hard-refresh; the new header chrome should appear once (no duplicate old header) and dog cards should populate. Open /adopt/ and /events/; both must render pages, not errors. View page source and Ctrl-F for \"pomdr-tokens-css\" (must exist) and for \"newpomdr-local\" (must be zero hits). Confirm menu/social icon glyphs render instead of empty boxes. Andrew then runs the full 40-URL crawl greping each page for HTTP 200, id='pomdr-tokens-css', and zero 'newpomdr-local'.\n\nRemedy A (fallback only): make a FRESH export from local (the Aug 25 one is a week stale), import with Unlimited on target, and STILL do Steps 3 to 6, because the export embeds local's et-cache. DB-level search-replace (dossier C) is demoted to optional hygiene: I verified the target DB page content is already clean (0 hits of newpomdr-local across all 100 pages via REST). If run anyway, search for the bare hostname 'newpomdr-local.local' -> 'new.pomdr.org'; the bare host contains no slashes so it catches http://, https://, protocol-relative, and JSON-escaped variants in one pass. Dry-run first.",
  "corrections": [
   "Leak location is wrong in the dossier. The protocol-relative //newpomdr-local.local URLs are NOT in the linked cached/dynamic CSS files: I fetched all three et-cache stylesheets referenced by the homepage (et-divi-dynamic-tb-145-tb-307-54.css, et-core-unified-54.min.css, et-core-unified-tb-145-tb-307-deferred-54.min.css) and each has ZERO occurrences. The leak lives in the INLINE <style id=\"divi-dynamic-critical-inline-css\"> block Divi prints into every page's HTML, whose source is the carried cache FILE wp-content/et-cache/54/et-divi-dynamic-tb-145-tb-307-54-critical.css (I fetched it on the target: 200, 41356 bytes, 2 @font-face groups for ETmodules and FontAwesome, Last-Modified Aug 25 18:28:06 GMT, matching the ?ver=1787682486 stamp).",
   "The mechanism 'ai1wm's URL rewrite missed the protocol-relative variant' is wrong. The target DB is clean: 0 occurrences of newpomdr-local across the rendered content of all 100 pages via /wp-json/wp/v2/pages. ai1wm rewrites only the database, never wp-content files, and the carrier here is a copied cache file. Consequence: the dossier's remedy C search-replace will NOT fix the icon fonts. Purging/regenerating Divi's static CSS is the fix; search-replace is optional hygiene for options/widgets/TB records my REST probe could not cover.",
   "Remedy A does not make C unnecessary and is itself a re-poisoning vector: a fresh ai1wm export carries local's wp-content/et-cache (with //newpomdr-local.local baked in by Divi's deliberately protocol-relative cache writer), so the et-cache purge is mandatory after EVERY remedy path, A included.",
   "Remedies B and D's 'ship a theme zip' instructions miss the folder-slug trap: the active theme folder on the target is divi-child, the repo folder is divi-child-integration. A zip whose top-level folder is not exactly divi-child installs as a SECOND theme; the 'Replace active with uploaded' flow never appears and the deploy silently no-ops.",
   "The dossier's D item 'bump Version 1.0.2 -> 2.0.0' is understated as hardening; it is a precondition. I read the repo's divi-child-integration/style.css: it STILL declares Version 1.0.2 and Theme Name 'Divi Child', identical to the old April file. Old and new theme are indistinguishable in the admin, in the replace-flow comparison table, and in the style.css?ver=1.0.2 cache-buster, which is one reason this failure sat invisible for a week and why caches would keep serving the old style.css after a correct replace.",
   "The dossier's mental model of the enqueue dependency chain (tokens->design->pomdr->boost->pages->boost-chrome->chrome) does not govern the rendered page. Divi's et_requeue_child_theme_styles (wp_enqueue_scripts priority 99999999, Divi/core/functions.php line 931) deregisters and re-enqueues EVERY child-theme stylesheet with its deps force-replaced by array('divi-style-parent-inline'), so print order equals wp_enqueue_style CALL order in inc/enqueue.php, not the declared deps. I verified via wp eval on local that pomdr-tokens/design/shared/boost/chrome/a11y all end up with deps=[divi-style-parent], and via the rendered local HTML that pomdr-native-boost-chrome prints BEFORE pomdr-chrome, the opposite of what its dep array (pomdr-native-pages, pomdr-chrome) requests. The verified-good local look is built on call order; treat the enqueue call sequence as load-bearing."
  ],
  "additions": [
   "Drop the 'divi-style' dependency from pomdr-tokens in inc/enqueue.php line 34. The chain currently survives by two Divi internals canceling out: et_divi_replace_parent_stylesheet (priority 99999998, Divi/functions.php line 479) DEREGISTERS the 'divi-style' handle our own dt_enqueue_styles registered (any handle whose src matches Divi/style*.css is collapsed into divi-style-parent), then et_requeue_child_theme_styles (99999999) rewrites our deps to divi-style-parent-inline, masking the now-dangling reference. I confirmed via wp eval on local: divi-style registered = NO after the cycle. If Divi ever drops or reorders the requeue flattening (or the code runs in a context where it does not), WordPress silently skips any queued style with an unregistered dep and every branch depending on it (wp-includes/class-wp-dependencies.php all_deps: 'continue' at top level, only a _doing_it_wrong since 6.9.1), killing ALL pomdr CSS site-wide with no error. Replace array('divi-style','pomdr-fonts') with array('pomdr-fonts'); ordering after Divi is already guaranteed by our priority-20 hook and by Divi's own requeue. Zero behavior change today, removes the worst silent single point of failure.",
   "Align every dep array in inc/enqueue.php with the actual call order (each handle depending on the previously enqueued pomdr handle, tracked through the conditional file_exists branches). Today deps are decorative because Divi discards them; if Divi ever stops discarding, the current arrays would REORDER the cascade (pomdr-chrome would jump before native-boost-chrome) and silently shift the design. Make declared order and call order identical so both regimes render the same.",
   "Answer to the seat question 'what if Divi inlines its stylesheet': it already does, unconditionally. et_core_is_inline_stylesheet_enabled in Divi/core/functions.php contains a TODO that force-returns true on the front end, so the parent CSS always ships as the divi-style-parent-inline inline block and no divi-style link tag ever exists. Never key a health check or a dependency on a divi-style link element.",
   "Sidecar guards, precise target: page-adopt.php already guards its LOCAL helpers (function_exists at lines 20 and 65) but calls pom_render_dog_card() unguarded at lines 237 and 277, and page-events.php calls pomdr_render_events() unguarded at lines 33 and 41; these exact lines are the observed 500s. Cheapest uniform fix: at the top of every sidecar, if ( ! function_exists( 'pom_render_dog_card' ) ) { include get_template_directory() . '/page.php'; return; } so a partial deploy degrades to an ugly-but-alive Divi page instead of a fatal.",
   "Render-time self-checks (cheap, concrete): (1) add --pom-build: \"2.0.0\" to :root in tokens.css and a 10-line probe in a11y.js (already zero-dependency, loads on every page) that reads getComputedStyle(document.documentElement).getPropertyValue('--pom-build'), console.errors when empty, and injects a fixed red banner only when body.admin-bar is present, so staff see design-layer death instantly and visitors never do; (2) define POMDR_BUILD in functions.php and print <meta name=\"pomdr-build\"> from inc/enqueue.php's wp_head hook, plus an HTML comment stamp in each sidecar, so one curl+grep proves PHP layer and template layer are the same build; (3) the deploy checklist one-liner: for each of the 40 URLs, grep for pomdr-tokens-css (present), newpomdr-local (absent), and the build meta.",
   "Unify the filemtime() pattern in inc/enqueue.php: the always-on styles (tokens, pomdr-design, pomdr, pomdr-chrome, a11y, and the js files) call filemtime() unguarded while the native-* ones are file_exists-guarded. On a partial deploy a missing file emits PHP warnings and a broken ver. One tiny helper that file_exists-checks and no-ops keeps partial deploys warning-free and page-safe.",
   "Cloudflare is in front of the target (cf-cache-status: HIT, age 512, max-age 14400 on theme CSS). Post-deploy verification can read stale for up to 4 hours; purge Cloudflare (or verify with ?nocache=1 query-string busting) before declaring success. The version bump to 2.0.0 also busts style.css?ver=1.0.2 through every cache layer.",
   "Divi-5 cross-host trap to add to Q4's list: et-cache is environment-poisoned by design. Divi's cache writer deliberately stores protocol-relative host URLs in the generated critical CSS files so one cache serves http and https, which means ANY host migration (export/import, rsync, staging pull) silently carries the old hostname until et-cache is purged. Add 'delete wp-content/et-cache after any cross-host move' to README-DEPLOY as a standing rule, and note the tell: the ?ver= on et-cache links is the generation epoch (1787682486 = Aug 25 18:28 GMT here), so a stale ver instantly dates the cache."
  ],
  "verified_claims": [
   "Dossier 3 (target runs old functions.php, only legacy enqueue): curl -sL https://new.pomdr.org/ then grep for stylesheet links; the only theme style is divi-child/style.css?ver=1.0.2 (id child-style-css) plus Divi et-cache links; no pomdr-* handles present. Local comparison via curl http://newpomdr-local.local/ shows the full chain (pomdr-fonts, pomdr-tokens, pomdr-design, pomdr-shared, native-boost, native-pages, native-boost-chrome, pomdr-chrome, pomdr-a11y, pomdr-home, paw-trail).",
   "Dossier 3 (old style.css kept): curl -sI and -s https://new.pomdr.org/wp-content/themes/divi-child/style.css returns 6693 bytes, Version 1.0.2 header, Last-Modified Wed 15 Apr 2026 21:14:06 GMT; repo copy is 17205 bytes.",
   "Dossier 3 (new files byte-identical): curl https://new.pomdr.org/wp-content/themes/divi-child/assets/css/pomdr.css (57196 bytes) and diff against /Users/kglocker/Documents/Antigravity/pomdr-website-handoff/divi-child-integration/assets/css/pomdr.css: IDENTICAL. Also probed inc/enqueue.php, inc/chrome.php, inc/native-gate.php, inc/videos.php (all 200 zero-byte ABSPATH-guard exits proving presence in the ACTIVE divi-child folder), page-native.php (0-byte 500, exists), tokens.css (3253 bytes, Last-Modified Aug 25 18:15:50 GMT).",
   "Dossier 4 (hard 500s and empty islands): curl -w %{http_code} https://new.pomdr.org/adopt/ -> 500 with 135524 bytes of partial HTML, /events/ -> 500; grep -c dog-card on the target homepage -> 0. Traced the exact fatal sites in the repo: page-adopt.php lines 237/277 call pom_render_dog_card() unguarded, page-events.php lines 33/41 call pomdr_render_events() unguarded.",
   "Dossier 4 (icon-font leak), independently re-verified and RELOCATED: grep of target homepage HTML finds the //newpomdr-local.local @font-face URLs inside <style id=\"divi-dynamic-critical-inline-css\"> (41408 chars), while all three linked et-cache CSS files contain ZERO occurrences (curl + grep each). Found the physical carrier by listing local disk (/Users/kglocker/Local Sites/newpomdr-local/app/public/wp-content/et-cache/54/ shows et-divi-dynamic-tb-145-tb-307-54-critical.css as the only leak-bearing file) and then fetching the SAME path on the target: https://new.pomdr.org/wp-content/et-cache/54/et-divi-dynamic-tb-145-tb-307-54-critical.css -> 200, 41356 bytes, 2 hits, Last-Modified Aug 25 18:28:06 GMT (equals the ?ver=1787682486 epoch on every et-cache link). Leak confirmed on all 5 additional crawled pages (/about/, /donate/, /foster-needs/, /adopt/, /events/, 2 hits each).",
   "DB cleanliness (new evidence): curl 'https://new.pomdr.org/wp-json/wp/v2/pages?per_page=100&_fields=id,link,content' (509KB) -> 0 occurrences of newpomdr-local, proving the leak is not database content and search-replace alone cannot fix it.",
   "Enqueue fragility mechanics (my seat's core question), verified from source and runtime: read Divi/functions.php lines 430-560 (et_divi_enqueue_stylesheet uses handle divi-style-parent when a child theme is active; et_divi_replace_parent_stylesheet at 99999998 deregisters src-matching handles; et_divi_print_stylesheet inlines as divi-style-parent-inline), Divi/core/functions.php line 931 (et_requeue_child_theme_styles force-replaces all child-theme style deps with divi-style-parent[-inline]) and line 1033 (et_core_replace_enqueued_style dequeues+deregisters then re-enqueues), plus wp-includes/class-wp-dependencies.php all_deps (missing dep = silent skip). Runtime confirmation via Local's bundled PHP + wp-cli.phar (mysqld socket XPO_ZHgiG): after do_action('wp_enqueue_scripts'), divi-style registered = NO, and pomdr-tokens/design/shared/native-boost/chrome/a11y/child-style all show deps=[divi-style-parent], proving our declared dep arrays are discarded every request and print order is enqueue call order (matches the rendered local HTML, where native-boost-chrome precedes pomdr-chrome despite its dep array)."
  ]
 },
 {
  "findings": "[]",
  "verdict": "sound-with-amendments",
  "diagnosis_assessment": "The partial-merge diagnosis is airtight for the on-disk state, and I hardened it against the two alternatives that could have survived. (1) CDN staleness: I fetched style.css with a random cache-buster query; Cloudflare reported cf-cache-status: MISS (origin fetch) and the origin still served the 6693-byte file with Last-Modified Wed, 15 Apr 2026, while the repo file is 17205 bytes. So the old file is on origin disk, not in a cache. (2) OPcache serving stale PHP over new disk files: opcache cannot explain a static CSS file, and style.css is old on disk, so the mixed state is proven regardless of opcache. (3) Two-theme-folders theory: killed, because the same /themes/divi-child/ path serves both the old style.css and the new assets/css/pomdr.css (200, 57196 bytes, matching the repo byte count). Timestamps corroborate a single merge event: the new files' Last-Modified (Aug 25 18:15 GMT) and the DB-stored et-cache version param on target (1787682486 = 2026-08-25 18:28:06 UTC) bracket the same half hour. What remains open is only the merge MECHANISM (skip-existing extract vs interrupted overwrite), which does not change the remedy. The one probe that would settle it needs host file-manager access: list mtimes inside wp-content/themes/divi-child; if every kept-old file predates Apr 16 2026 and every new file clusters at Aug 25 ~18:15, it was skip-existing merge semantics; a crashed overwrite would show a partial, extraction-order pattern of updated pre-existing files. One sub-claim I would soften: a COMPLETED ai1wm import replaces files wholesale, so the coworker's actual path was almost certainly not a finished ai1wm run at all (more likely DB import plus a file-manager/FTP merge); the dossier already leans this way and is right to.",
  "remedy_ranking": "RANK 1: Remedy B (surgical theme replacement), with two Divi-critical amendments. Amendment one: the upload zip's top-level folder MUST be named exactly divi-child. The repo folder is divi-child-integration (locally it works only because the active theme is a symlink named divi-child). A zip rooted at divi-child-integration installs as a SECOND theme, the broken divi-child stays active, and the fix silently does nothing. Andrew builds it: cp -R divi-child-integration ~/Desktop/divi-child; cd ~/Desktop; zip -rq divi-child.zip divi-child. Amendment two: bump Version: 1.0.2 to 2.0.0 in style.css BEFORE zipping. Both sides are currently 1.0.2, so WP's replace screen would show identical versions and, worse, Cloudflare (cf-cache-status/max-age=14400 observed) could serve the stale style.css?ver=1.0.2 URL for up to 4 hours after the fix; a new ?ver=2.0.0 sidesteps that with zero purging. Coworker click path, no CLI: 1. wp-admin on new.pomdr.org. 2. Appearance > Themes > Add New Theme > Upload Theme > Choose File (divi-child.zip) > Install Now. 3. On the \"already installed\" comparison screen click \"Replace current with uploaded\" (WP 5.5+ true replace: it deletes the old folder, never merges; target is WP 7.0.2 so it is present). 4. Confirm Themes shows Divi Child 2.0.0 Active. 5. REQUIRED, not optional: Divi > Theme Options > Builder tab > Advanced > Static CSS File Generation > click Clear, then Save Changes. Fallback if they cannot find it: host file manager, delete the folder wp-content/et-cache (it rebuilds on the next page views). This is the actual fix for the icon-font leak; see corrections. 6. Settings > Permalinks > Save Changes (no edits, just Save) to register pets/videos rewrites from the now-loaded inc/ files. 7. Verify: home styled with new chrome, no raw script band, view-source contains pomdr.css and does NOT contain maximum-scale; /adopt/ and /events/ return styled pages; dog cards present on home (local shows 12, target currently 0). If step 3 offers no Replace button or errors, fallback: host file manager, rename wp-content/themes/divi-child to divi-child-old, extract divi-child.zip into wp-content/themes/, verify, then delete divi-child-old; do the rename and extract back to back because the site can fall back to a default theme while the active folder is missing. RANK 2 (after B is verified): remove the Theme Builder header and footer rather than fixing the mangled record. Reason: the band is a Code module (et_pb_code_0_tb_header, in the header layout, TB layout ids tb-145/tb-307 per the et-cache filenames) holding the legacy LGL adoption-iframe prefill script (field_21/dogname) with double-mangled JSON escapes (u003c, u0022, bare n; the backslashes were stripped, so no automatic re-decode is possible). \"Fixing\" it would re-arm a display:none-hidden script that fights the child theme's own prefill wiring; removal is strictly better. Steps: Divi > Theme Builder > portability (up/down arrows) icon top right > Export tab > export a JSON backup first. Then on the Default Website Template card hover Global Header > trash icon > confirm; same for Global Footer; leave any Custom Body area alone; check no other template cards exist; click Save Changes. Safe because pomdr-chrome.css (lines 9-10) hides BOTH the TB chrome (.et-l--header/.et-l--footer) AND the classic Divi chrome that replaces it (#main-header, #top-header, #et-secondary-nav, #main-footer, #footer-bottom), and both sidecar templates and page-native.php take chrome the same way (get_header/get_footer plus that CSS). Net effect: if design CSS ever fails to load again, visitors see a benign classic Divi nav instead of a garbage script band. RANK 3: data hygiene (dossier C, amended): wp search-replace for //newpomdr-local.local and http(s) variants is belt-and-braces for DB rows only; it does NOT fix the icon fonts (see corrections). Andrew can run it later via CLI, or a Better Search Replace plugin pass if plugin installs are acceptable on the disposable test bed. RANK 4 (fallback only): Remedy A, full ai1wm re-import with Unlimited. Demote it: it costs the paid extension, wipes a DB that is already proven perfect, and STILL requires the Divi cache clear afterward because ai1wm restores wp-content/et-cache verbatim and rewrites only the database, so the local-URL leak survives a flawless import. Use A only if B's verification fails.",
  "corrections": [
   "The icon-font leak mechanism is misattributed. The dossier says ai1wm's URL rewrite missed the protocol-relative variant, implying a DB rewrite gap. I located the leak: 23 occurrences of //newpomdr-local.local/wp-content/themes/Divi/core/admin/fonts/... sit inside the server-side-inlined <style id=\"divi-dynamic-critical-inline-css\"> block (Divi's critical CSS cache layer, read from wp-content/et-cache files at render time), while the linked et-cache files I fetched directly (et-core-unified-tb-145-tb-307-deferred-54.min.css and et-divi-dynamic-tb-145-tb-307-54.css) contain zero local refs. ai1wm never rewrites files at all, only the DB, so this is carried-over cache files with baked local URLs, not a missed rewrite pattern. Consequence: remedy C's wp search-replace will NOT fix the icon fonts; clearing the Divi static CSS cache will, and even remedy A (perfect re-import) reproduces the leak until that clear happens.",
   "The remedy D line 'sidecars vs native both suppress TB differently (risk analysis needed)' rests on a wrong premise. Both sidecar templates (all 41 call get_header()) and page-native.php use identical chrome handling: get_header()/get_footer() plus pomdr-chrome.css display:none rules. That CSS (lines 9-10) already hides the classic Divi header/footer (#main-header, #top-header, #et-secondary-nav, #main-footer, #footer-bottom) that Divi falls back to when TB layouts are deleted, so TB header/footer removal is low risk and needs no dual-path analysis, just a TB JSON export backup and leaving any body layouts alone.",
   "The band's description as 'JSON-escaped script text' understates the damage: the content is DOUBLE-mangled (u003c and u0022 without backslashes, literal n instead of newlines). The JSON escapes themselves lost their backslashes, so the record cannot be repaired by decoding; the module content must be re-entered by hand or the module deleted. This matches the D4-to-D5 Code-module entity mangling the repo already works around for its own islands (commit 48e7472).",
   "Remedy B as written omits that the uploaded zip's root folder must be named divi-child. The repo folder is divi-child-integration; zipping it as-is installs a second, inactive theme and the fix silently no-ops. Also, both old and new style.css declare Version 1.0.2, so without the 2.0.0 bump the replace flow gives no visible confirmation and Cloudflare can serve the stale style.css?ver=1.0.2 for up to 4 hours (observed cache-control: max-age=14400).",
   "Minor framing: the deploy-method inference correctly rules out a completed ai1wm import, but the dossier still calls the archive 'imported'. The DB-side et-cache version param on target decodes to 2026-08-25 18:28:06 UTC and the new files' Last-Modified is Aug 25 18:15 GMT, one event window; whatever tool ran, files were merged with keep-existing semantics in that window while the DB landed whole."
  ],
  "additions": [
   "Deploy zip build rule (hardening, do before redeploy): ship the handoff zip with top-level folder divi-child, Version 2.0.0, built by copying divi-child-integration to a folder named divi-child and zipping that. Document in README-DEPLOY next to the existing 'delete theme folder first, never merge' line.",
   "Divi cache clear is a REQUIRED post-deploy step for EVERY remedy including a perfect ai1wm re-import, because et-cache files are restored verbatim with baked local URLs. Exact non-developer click path: wp-admin > Divi > Theme Options > Builder tab > Advanced sub-tab > Static CSS File Generation > Clear button > Save Changes. Fallback that always works regardless of Divi UI version: host file manager, delete wp-content/et-cache entirely; it self-rebuilds. Add to README-DEPLOY.",
   "Prevent the leak at the source: clear et-cache on the LOCAL site immediately before creating any future ai1wm export, so archives never ship critical CSS with //newpomdr-local.local URLs baked in.",
   "Cloudflare fronts new.pomdr.org (cf-cache-status and cf NEL headers observed). After any fix, either purge Cloudflare or rely on changed ?ver params; the only URL at risk without a purge is the unchanged style.css?ver=1.0.2, which the 2.0.0 bump retires.",
   "TB header/footer disposal decision (answering dossier D): remove, do not repair. The header Code module is the legacy LGL adoption-iframe prefill script; repairing it re-arms an invisible script that duplicates the child theme's own prefill wiring (already wired 3 ways per project notes). Export TB JSON backup first via the Theme Builder portability icon, delete only the Global Header and Global Footer areas of the Default Website Template, keep body layouts, Save. Do this AFTER the theme redeploy is verified so pomdr-chrome.css is confirmed loading. Deleted layouts are also recoverable from the trashed et_header_layout/et_footer_layout posts.",
   "Sidecar degradation guard, sharpened: /adopt/ emitted 135KB of HTML before the fatal, so a mid-page function_exists check is too late for clean error handling (headers and half the page already sent). Put one guard at the TOP of each sidecar before get_header(): if pom_render_dog_card (or a single POMDR_BUILD constant) is undefined, render a minimal styled notice and return. Cheap, do before redeploy.",
   "Ten-second staleness canary for non-developers, complementing the build-constant idea: view-source of the home page and search for 'pomdr.css' (must exist) and 'maximum-scale' (must NOT exist). Both flip only when the new functions.php is actually executing. Put this in the coworker checklist.",
   "OPcache caveat for managed hosts: if after the Replace flow the page source still shows ver=1.0.2 despite new files on disk, ask the host to flush PHP OPcache (some hosts run opcache.validate_timestamps=0). Verification catches it via the canary above.",
   "After redeploy, open Divi > Theme Builder once to confirm template assignments survived (they live in the DB and should be untouched by a file-level theme replace; the check costs seconds and catches surprises before stakeholders see the site)."
  ],
  "verified_claims": [
   "Partial merge root cause: cache-busted curl of https://new.pomdr.org/wp-content/themes/divi-child/style.css?cb=<random> returned 6693 bytes, Last-Modified Wed, 15 Apr 2026 21:14:06 GMT, cf-cache-status: MISS (origin disk, CDN excluded); repo divi-child-integration/style.css is 17205 bytes (wc -c). Same folder simultaneously serves the NEW assets/css/pomdr.css (200, 57196 bytes, exactly the dossier's byte count). Old and new files coexist in one theme folder on origin: proven.",
   "Old functions.php behavior on target: curl of https://new.pomdr.org/ shows style.css?ver=1.0.2 as the only divi-child stylesheet, the maximum-scale=1.0 viewport meta still present, and none of the new enqueues (pomdr-fonts, tokens.css, pomdr.css) in the HTML; repo inc/enqueue.php confirms those handles would print if the new functions.php (which require_once's inc/enqueue.php at lines 2277-2281) were running.",
   "The u003cscript band: extracted from the target home HTML inside <div class=\"et_pb_code_0_tb_header ... et_pb_code\"> and identified as the LGL adoption iframe prefill script (secure.lglforms.com, field_21, dogname param) with backslash-stripped JSON escapes; TB layout ids tb-145 and tb-307 read from the et-cache filenames. Also confirmed present exactly once on the known-good local home (curl http://newpomdr-local.local/) where pomdr-chrome.css?ver=1787681750 is enqueued and hides it, matching the dossier's latent-flaw claim.",
   "Icon-font leak location: grep of target home HTML found 23 //newpomdr-local.local/wp-content/themes/Divi/core/admin/fonts/... refs, all inside <style id=\"divi-dynamic-critical-inline-css\"> (position-indexed with perl); direct fetches of the linked et-cache files (et-core-unified-tb-145-tb-307-deferred-54.min.css, et-divi-dynamic-tb-145-tb-307-54.css) contain zero local refs. The leak lives in Divi's critical-CSS cache layer inlined server-side, not in DB rows.",
   "Hard 500s: curl -w reports /adopt/ = 500 with 135524-byte body and /events/ = 500 with 126991-byte body (partial HTML then fatal, as claimed); repo page-adopt.php calls get_header() then unguarded helper functions defined only in the new functions.php.",
   "Dynamic islands empty: target home has 0 'dog-card' occurrences vs 12 on the local home (grep -c on both curl results).",
   "Chrome suppression mechanics (basis for the TB-removal judgment): divi-child-integration/assets/css/pomdr-chrome.css lines 9-10 hide .et-l--header, #et-secondary-nav, #top-header, #main-header and .et-l--footer, #main-footer, #footer-bottom; all 41 sidecar page-*.php templates and page-native.php call get_header() (grep -l count), so TB suppression is CSS-only and identical across both template families."
  ]
 }
]