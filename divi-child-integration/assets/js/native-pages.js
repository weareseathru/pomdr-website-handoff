/* GENERATED by scripts/native/fidelity/gen-pages-js.mjs. Do not edit. */

/* ---- page: about ---- */
if (document.querySelector('.pg-about')) { (function () {
// Scroll effect
  window.addEventListener('scroll', () => {
    document.getElementById('site-nav').classList.toggle('scrolled', window.scrollY > 40);
  });

})(); }

/* ---- page: donate ---- */
if (document.querySelector('.pg-donate')) { (function () {
// Scroll effect
  window.addEventListener('scroll', () => {
    document.getElementById('site-nav').classList.toggle('scrolled', window.scrollY > 40);
  });
// Amount selector
const amountBtns = document.querySelectorAll('.amount-btn');
const customInput = document.getElementById('custom-amount');
const donateBtn = document.getElementById('donate-btn');
let selectedAmount = 125;

amountBtns.forEach(btn => {
  btn.addEventListener('click', () => {
    amountBtns.forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    selectedAmount = parseInt(btn.dataset.amount);
    customInput.value = '';
  });
});
customInput.addEventListener('input', () => {
  amountBtns.forEach(b => b.classList.remove('active'));
  selectedAmount = parseInt(customInput.value) || 0;
});
donateBtn.addEventListener('click', () => {
  const amt = customInput.value ? parseInt(customInput.value) : selectedAmount;
  if (amt > 0) {
    window.location.href = `/donation/?initialdonation=${amt}`;
  }
});

})(); }

/* ---- page: helping-paw ---- */
if (document.querySelector('.pg-helping-paw')) { (function () {
// Scroll effect
  window.addEventListener('scroll', () => {
    document.getElementById('site-nav').classList.toggle('scrolled', window.scrollY > 40);
  });

})(); }

/* ---- page: home ---- */
if (document.querySelector('.pg-home')) { (function () {
(function(){ var b=document.getElementById('promo-banner'); if(!b) return;
      if(localStorage.getItem('pomdrPromoDismissed')==='1') b.classList.add('dismissed');
      var x=b.querySelector('.promo-dismiss');
      if(x) x.addEventListener('click', function(){ b.classList.add('dismissed'); try{localStorage.setItem('pomdrPromoDismissed','1');}catch(e){} });
    })();

})(); }

/* ---- page: surrender ---- */
if (document.querySelector('.pg-surrender')) { (function () {
// Scroll effect
  window.addEventListener('scroll', () => {
    document.getElementById('site-nav').classList.toggle('scrolled', window.scrollY > 40);
  });

})(); }

/* ---- page: volunteer ---- */
if (document.querySelector('.pg-volunteer')) { (function () {
// Scroll effect
  window.addEventListener('scroll', () => {
    document.getElementById('site-nav').classList.toggle('scrolled', window.scrollY > 40);
  });

})(); }
